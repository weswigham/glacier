language: node_js
node_js:
  - '6'

os:
  - linux

branches:
  only:
    - master

script: sh -e ./scripts/travis.sh

test: 
cache:
  directories:
    - glacier/node_modules
    - gluon/node_modules

notifications:
  email:
    on_success: never
    on_failure: change

env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
